apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # ArgoCD projects for RBAC separation
  # Note: GitOps operator and ArgoCD instance assumed to be pre-installed
  - ../../components/argocd/projects/overlays
  # Root application for nightly cluster
  - cluster-config-app.yaml
