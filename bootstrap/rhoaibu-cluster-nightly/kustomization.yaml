apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Base bootstrap (GitOps operator + ArgoCD instance)
  - ../../components/operators/openshift-gitops-operator/operator
  - ../../components/operators/openshift-gitops-operator/instance
  - ../../components/argocd/projects/overlays
  # Root application for nightly cluster
  - cluster-config-app.yaml
